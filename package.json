{
  "name": "st-of-chengming",
  "private": true,
  "version": "1.0.1",
  "description": "ST Launcher — Tauri + Leptos (Rust). npm scripts wrap cargo tauri.",
  "scripts": {
    "dev": "cargo tauri dev",
    "build": "cargo tauri build",
    "tauri": "cargo tauri"
  }
}
